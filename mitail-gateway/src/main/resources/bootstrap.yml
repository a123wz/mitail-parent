server:
  port: 9001
spring:
  application:
    name: mitail-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 10.64.14.111:9200
      config:
        server-addr: 10.64.14.111:9200
        file-extension: yaml #格式
#        prefix: nacos-provider #
    sentinel:
      transport:
        port: 8719
        dashboard: localhost:8080

#fegin调用流控
feign.sentinel.enabled: true


